import type { BreakpointTokens } from './types';

/** Breakpoint tokens (numeric pixel values) */
export const breakpoints: BreakpointTokens = {
  xs: 0,
  sm: 480,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
};
